require 'spec_helper'

describe CodeCommenter do
  it 'has a version number' do
    expect(CodeCommenter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
